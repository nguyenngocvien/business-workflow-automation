apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: workflow-service-prod
  namespace: argocd

spec:
  project: backend-services

  source:
    repoURL: https://github.com/nguyenngocvien/business-workflow-automation.git
    targetRevision: main
    path: gitops/apps/workflow-service

    helm:
      valueFiles:
        - ../../environments/prod/workflow-service/values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
