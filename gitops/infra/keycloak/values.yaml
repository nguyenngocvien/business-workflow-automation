keycloak:
  auth:
    adminUser: admin
    adminPassword: admin

  externalDatabase:
    host: postgres-postgresql.infra.svc.cluster.local
    port: 5432
    user: postgres
    password: postgres
    database: keycloak

  production: false

  service:
    type: ClusterIP